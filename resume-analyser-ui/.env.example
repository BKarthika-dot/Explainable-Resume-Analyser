# Base URL of the FastAPI backend (modules/main.py).
# In production this should be the public URL of your deployed API,
# e.g. https://api.your-domain.com — and must be included in that
# service's FRONTEND_ORIGINS env var so CORS allows it.
VITE_API_URL=http://127.0.0.1:8000
